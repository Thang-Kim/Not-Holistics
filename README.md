🔑 Generate a Fly.io API Token
Go to Profile → Settings → Access Tokens in Fly.io  (the page you have open).

Choose App Deploy Token from the dropdown.

Give it a name (e.g., github-deploy).

Select the app you want to connect.

Set expiry (default is 100y, which is fine for long-term use).

Click Create Token and copy the generated token.
